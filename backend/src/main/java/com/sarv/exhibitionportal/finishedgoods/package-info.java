/**
 * Snapshot of pharma-erp finished goods used on the buy catalogue.
 * Staff sync pulls from {@code pharmadb} when {@code exhibition.pharma-erp.enabled=true}.
 * Credentials alone do not fill the list — sync (or restart) must run.
 */
package com.sarv.exhibitionportal.finishedgoods;
