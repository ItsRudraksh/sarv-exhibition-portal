/**
 * SPA path forwards so a packaged JAR can deep-link {@code /staff}, {@code /admin},
 * and {@code /web} to {@code index.html}. API routes stay on {@code /api/v1}.
 */
package com.sarv.exhibitionportal.web;
