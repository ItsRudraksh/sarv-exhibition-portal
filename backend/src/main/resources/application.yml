spring:
  application:
    name: exhibition-portal
  datasource:
    url: ${DATASOURCE_URL:jdbc:mysql://localhost:3306/exhibition_portal?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8}
    username: ${DATASOURCE_USERNAME:exhibition}
    password: ${DATASOURCE_PASSWORD:exhibition}
  flyway:
    enabled: true
    locations: classpath:db/migration
  servlet:
    multipart:
      max-file-size: 21MB
      max-request-size: 21MB

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

exhibition:
  poc: true
  default-campaign-id: 22222222-2222-4222-8222-222222222222
  cors-origins:
    - http://localhost:5173
    - https://localhost:5173
    - http://127.0.0.1:5173
    - https://127.0.0.1:5173
    - https://192.168.1.12:5173
  storage-root: ${EXHIBITION_STORAGE_ROOT:./var/exhibition-files}
  file-retention-days: 730
  card-max-bytes: 10485760
  catalogue-max-bytes: 20971520
  consent-policy-version: card-extraction-v1
  export-retention-hours: 24
  outbox:
    schedule-enabled: true
    max-attempts: 5
    backoff-seconds: 30
    marketing-destination: poc-mailbox
    vendor-destination: poc-vendor-stub
    force-failure-code: ""
  staff-bootstrap-password: ${EXHIBITION_STAFF_BOOTSTRAP_PASSWORD:}
